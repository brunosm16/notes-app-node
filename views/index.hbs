<div class="container-fluid">
  <div class="row">
    <div class="col-12 btn-group-vertical" role="group">
      {{#each notesList}}
        <a
          class="btn btn-lg btn-block btn-outline-dark"
          href="/notes/view?id={{id}}"
        >
          {{header}}
        </a>
      {{/each}}
    </div>
  </div>
</div>